services:
  db:
    image: postgres
    container_name: postgres-dev
    environment:
      POSTGRES_DB: url_shortener
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: mysecretpassword
    ports:
      - "5432:5432"

volumes:
  postgres_data: